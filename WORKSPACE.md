# 🧪 لعبة-next — مساحة العمل للإضافات الجديدة

> ⚠️ **هذا مستودع عمل مستقل — لا تلمس «لعبة-dev»**

- «لعبة-dev» = المرجع الثابت الحالي (الوضع المستقر، tag `ref-v5-final`).
- «لعبة-next» = نسخة كاملة منه تُستخدم **فقط** للإضافات الجديدة (v6+) — أي تعديل هنا لا يمس المرجع بأي شكل.
- فرع العمل: `feature-workspace` (يبدأ من `ref-v5-final` بالضبط).
- قواعد الإضافات الجديدة (نفس القاعدة الذهبية في README):
  1. الفحوصات الثلاثة لازم تفضل خضرا (61 + 77 + 71).
  2. أي إضافة تتوفر على المنصتين: ويب + أندرويد.
  3. أحداث Socket تتسجل في `EVENTS` داخل `build-apk-www.js`.
  4. أي معالج سوكيت يعمل `ack` حتى عند الرفض.
  5. عند اكتمال الإضافة: نبني APK جديد هنا + نوثّق، ولا داعي للمس المرجع إطلاقًا.